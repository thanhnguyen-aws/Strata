procedure main() {
    var a: int := 10;
    var b: int := 10;
    assert a == b;

    var x: int := 5;
    var y: int := 3;
    assert x > y;
    assert y < x;
    assert x >= 5;
    assert y <= 3;
}
