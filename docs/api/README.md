# API-Level Documentation for Strata

Run the following commands in this directory to generate a webpage
(default location `./.lake/build/doc/index.html`):

```
lake build Strata:docs StrataTest:docs
```
